    <br>
    <% @todos.each do |todo| %>
        <div class='card card-body bg-light'>
            <%= todo.description %>
            <p> ¿Completada?: 
                <% if todo.completed == true %> 
                Si, por supuesto.
                <% else %>
                No, pendiente.
                <% end %> </p>
        </div>
    <% end %>
    
    <br><%= link_to 'Crear ToDo', todos_new_path, class: 'btn btn-secondary' %>